{
  "tap": "extend-commerce",
  "category": "WMS & Fulfilment",
  "install_uri": {
    "env": "git+https://github.com/joaoraposooptiply/Op-Integrations.git#subdirectory=taps/tap-extend"
  },
  "connect_ui_params": {
    "username": {
      "label": "Username",
      "description": "Your Extend Commerce API username."
    },
    "password": {
      "label": "Password",
      "description": "Your Extend Commerce API password.",
      "type": "password"
    },
    "client": {
      "label": "Client Shortname",
      "description": "Your client identifier in Extend Commerce. Example: MAXGAMING"
    },
    "warehouse_codes": {
      "label": "Warehouse Codes",
      "description": "Comma-separated list of warehouse codes to sync. Leave empty to sync all. Exclude RMA/returns warehouses (e.g. MAXRMA).",
      "type": "text",
      "required": false
    },
    "sync_leadTime_supProducts": {
      "label": "Sync Lead Time from Supplier Agreements?",
      "description": "If set to 'true', lead times (manufacturing, transport, customs) will be imported from Extend to Optiply for supplier products.",
      "type": "boolean",
      "required": false
    }
  },
  "config": {
    "start_date": "2010-01-01T00:00:00Z",
    "api_url": "https://s05.extend.se/RESTAPI",
    "warehouse_codes": null
  },
  "label": "Extend Commerce",
  "icon": "https://s3.amazonaws.com/cdn.hotglue.xyz/images/logos/extend-commerce.svg",
  "domain": "extend.se",
  "isForked": true,
  "validation_supported": false,
  "default_import_scheduler": "*/15 * * * *",
  "default_export_scheduler": "*/10 * * * *"
}
